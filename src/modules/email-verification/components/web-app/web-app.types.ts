export type DeployAccountMessage = {
  accountAddress: string;
  publicKey: string;
  fullPublicKey: string;
  privateKey: string;
  encryptedPrivateKey: string;
  accessToken: string;
  baseApiUrl: string;
};
